export {};

const r: number = 5;
const h: number = 10;

const volume: number = Math.PI * (r ** 2) * h;

console.log(`Raio: ${r} | Altura: ${h}`);
console.log(`Volume do cilindro: ${volume.toFixed(2)}`);
